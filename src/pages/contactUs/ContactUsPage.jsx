import React from 'react';
import ContactUs from '../../components/contactUs/ContactUs'; // Adjust path if needed

function ContactUsPage() {
  return (
    // The component itself is full-width, so no container is needed here
    <ContactUs />
  );
}

export default ContactUsPage;